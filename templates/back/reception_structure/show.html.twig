{% extends 'base.html.twig' %}

{% block title %}ReceptionStructure{% endblock %}

{% block body %}
    <h1>ReceptionStructure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reception_structure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reception_structure.name }}</td>
            </tr>
            <tr>
                <th>SlugName</th>
                <td>{{ reception_structure.slugName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ reception_structure.createdAt ? reception_structure.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ reception_structure.updatedAt ? reception_structure.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_reception_structure_index') }}">back to list</a>

    <a href="{{ path('app_back_reception_structure_edit', {'id': reception_structure.id}) }}">edit</a>

    {{ include('back/reception_structure/_delete_form.html.twig') }}
{% endblock %}
